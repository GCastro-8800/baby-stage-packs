
User wants GA4 tag installed. Simple: add the gtag snippet to `index.html` right after `<head>`. SPA — gtag config will track initial pageview; route changes need manual tracking but that can be a follow-up. Keep plan minimal.
