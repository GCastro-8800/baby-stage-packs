
CookieYes is already first. Need to insert Consent Mode v2 defaults block between CookieYes and GA4 loader. Current GA4 block already declares `gtag` function and dataLayer — need to avoid duplicate declarations. Solution: move the dataLayer/gtag function declaration into the new consent block (before GA4 src), and remove the duplicate declaration from the GA4 block (keep only `gtag('js', ...)` and `gtag('config', ...)`).
