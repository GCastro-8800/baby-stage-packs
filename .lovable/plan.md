
User says Consent Mode defaults are missing. Let me check current index.html — per my own previous summary, it should already be there. User's requested layout differs slightly: they want gtag('js') and gtag('config') in the SAME inline block as consent defaults, and the gtag.js loader AFTER. Functionally equivalent but matches their exact spec. I'll verify current state then replace.
